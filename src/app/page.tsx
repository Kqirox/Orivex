import Hero from "@/components/landing/hero";
import WhyLearnault from "@/components/landing/why-learnault";

const Home = () => {
  return (
    <main>
      <Hero />
      <WhyLearnault />
    </main>
  );
};

export default Home;
